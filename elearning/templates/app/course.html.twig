{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} - {{ course.title }}{% endblock %}

{% block body %}

<h1>{{ course.title }}</h1>
<p>{{ course.content }}</p>

{% endblock %}
